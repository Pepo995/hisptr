export enum Skin {
  Dark = "dark",
  Bordered = "bordered",
  SemiDark = "semi-dark",
  Light = "light",
}

export enum UserType {
  SUPER_ADMIN = "superadmin",
  MEMBER = "member",
  PARTNER = "partner",
  PARTNER_USER = "partneruser",
  CUSTOMER = "customer",
}

export enum eventVenueDetailsCOI {
  send = "send",
  notRequire = "not_require",
  requireCustom = "require_custom",
  notSure = "not_sure",
}
